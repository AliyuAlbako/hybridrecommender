services:
  - type: web
    name: hybrid-recommender
    env: python
    plan: free
    buildCommand: |
      cd ecomm_recommender
      pip install -r ../requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn ecomm_recommend.wsgi:application --log-file -
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: ecomm_recommend.settings
      - key: PYTHON_VERSION
        value: 3.12.0
    autoDeploy: true
